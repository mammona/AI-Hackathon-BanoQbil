# V11 update

The current implementation is language-selected RAG. The API receives `language=english|urdu|punjabi` and uses only that language's concept embeddings. There is no runtime symptom translation. See `LANGUAGE_SELECTED_SYMPTOM_RAG_V11.md` for the current design.

---

# Superseded

See `CONTEXT_AWARE_SYMPTOM_RAG_V9.md` for the current implementation.
